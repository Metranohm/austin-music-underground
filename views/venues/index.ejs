<%- include('../partials/html-head') %>
<%- include('../partials/nav') %>

<section>
  <h1>Venues!</h1>
  <h2>All the Venues!</h2>
	<ul>
  <% venues.forEach(taco => { %>
    <li>🌮 <%= venue.name %> is <%= venue.tasty ? 'tasty' : 'not tasty' %></li>
  <% }) %>
  </ul>
</section>

<%- include('../partials/footer') %>